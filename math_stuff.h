#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

bool isPrime(int number);

bool primeCheck(int START_DIV, int number);

void printPrimes(int n);

void primeOutput(int START_DIV, int n);

int findFibo(int n);

void findFactors(int number);

void factorCheck (int START_DIVISOR, int number);
